Flask==3.1.3
flask-cors==6.0.5
Pillow==11.3.0
numpy==1.24.3
timm==1.0.27

# torch se instala aparte porque depende de si hay GPU o no:
#
# Con GPU NVIDIA (CUDA 11.8):
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118
#
# Sin GPU (CPU):
#   pip install torch==2.7.1
